# M3FX Project Plan

## Purpose

M3FX is a modular Material Design 3 component library for JavaFX. The project is still early enough to favor clean API and implementation shape over compatibility with earlier internal versions.

This file tracks product status and planning only. Repository rules, code style, nullability, documentation, Gradle invocation, and commit-message requirements belong in `AGENTS.md`.

## Current Baseline

- Java source and bytecode target: Java 17.
- Default JavaFX dependency for local builds and the demo app: JavaFX 21.
- Public implementation should stay compatible with JavaFX 14 APIs unless newer APIs are guarded by runtime checks or reflection.
- JavaFX is an application-owned dependency and is not published as an API dependency of the library.
- The JPMS module descriptor keeps transitive JavaFX readability because public APIs expose JavaFX types; this remains separate from Maven and Gradle artifact dependency publication.
- Material colors are generated through `org.glavo:MonetFX:0.4.0`.
- The demo app is a separate Gradle subproject.
- Theme and token APIs are token-first and profile-aware, with baseline M3 and M3 Expressive profiles represented separately.
- M3 Expressive currently has profile-specific color, typography, shape, component sizing, semantic motion scheme tokens, and motion behavior timings.
- M3 Expressive progress indicators use wavy linear and circular indicator geometry through profile-specific component tokens, and loading indicators use a dedicated animated shape sequence control with profile-specific metrics and default/contained variants.
- M3 Expressive action controls use profile-specific button padding, connected button padding and spacing, split button menu sizing, FAB extended padding and menu spacing, segmented button padding, icon toggle spacing, tab indicator metrics, icon sizes, chip padding, and chip group spacing.
- M3 Expressive inputs, selection controls, and sliders use profile-specific input padding, touch target, track thickness, and thumb sizing.
- M3 Expressive navigation and list components use profile-specific spacing, padding, and selected-container shape tokens.
- M3 Expressive menus, search surfaces, picker surfaces, and sheets use profile-specific padding, item spacing, result spacing, day/time cell sizing, popup sizing, and drag-handle metrics.
- M3 Expressive forms and validation summaries use profile-specific row spacing, section spacing, label width, row height, summary padding, and invalid-item geometry.
- M3 Expressive surfaces and carousels use profile-specific surface padding, container shape, carousel track padding, item spacing, item opacity, and selected-item shadow metrics.
- M3 Expressive cards, dialogs, snackbars, app bars, and toolbars use profile-specific container shape, padding, height, and content/action slot metrics.
- Theme installation marks roots with profile and brightness style classes so applications and demo pages can branch CSS for baseline, expressive, light, and dark modes without replacing the theme API.
- Scene-level theme installation follows runtime scene-root replacement, temporarily overlays the current root, restores any previous local root theme when the root leaves the scene, and clears scene-owned declarations when uninstalled.

## Architecture

- `org.glavo.m3fx.theme` owns theme creation, scene installation helpers, stylesheet installation, and token stylesheet generation.
- `org.glavo.m3fx.tokens` owns color, typography, shape, elevation, motion, state-layer, density, profile, and component token groups.
- Public theme and token abstractions use sealed interfaces; implementations live in internal packages.
- `org.glavo.m3fx.animation` owns reusable Material motion durations and easing curves.
- `org.glavo.m3fx.internal` owns shared runtime infrastructure such as theme resolution and generated stylesheet caching.
- `org.glavo.m3fx.controls` owns public controls, foundation primitives, and composition containers.
- `org.glavo.m3fx.skins` owns custom skins for layout, drawing, interaction, state layers, ripple, and animation behavior. Custom skins inherit JavaFX base skin classes such as `SkinBase`, `LabeledSkinBase`, or project skin bases. `PopupControl` skins use a project popup skin base because JavaFX `SkinBase` only accepts `Control` skinnables; tests reject concrete JavaFX skin inheritance and direct `Skin` implementations.
- Foundation primitives such as `M3Icon` and `M3Text` support components but are not standalone Material component pages.
- `M3Icon` is an icon glyph primitive for Material Symbols or fallback text. It is not focus-traversable by default; standard interactive component mappings belong to icon buttons, navigation items, app bars, menus, lists, and other controls that use icon slots.
- Public control APIs prefer constructors and explicit mutable properties over static convenience factories or narrow convenience setters. The controls package currently exposes no public `with`, `of`, or `create` factory methods, and count badges are created through constructors plus ordinary text mutation.
- Public component token records expose canonical record constructors only; source-style tests reject public token-record convenience constructor overloads so theme authors use explicit token values.
- Action, selection, menu, list, tab, navigation, container, feedback, and progress controls avoid inheriting from concrete JavaFX controls such as `Button`, `ToggleButton`, `CheckBox`, `RadioButton`, `MenuButton`, `ListView`, `Slider`, `ProgressBar`, `ProgressIndicator`, and `Tooltip`; tests enforce these boundaries. Text input controls intentionally retain JavaFX text input base classes to preserve editing, selection, clipboard, IME, and multiline behavior.
- Controls use per-control user-agent stylesheets where JavaFX supports them. Popup-only styling remains in dedicated control CSS files loaded through the base stylesheet.
- `M3ThemeManager` is a convenience installer, not a required runtime dependency for applications.
- The root style classes `m3-profile-baseline`, `m3-profile-expressive`, `m3-light`, and `m3-dark` describe the installed theme mode for application-level CSS.

## Implemented Areas

### Build And Distribution

- Multi-project Gradle build for the library and demo app.
- Java module descriptors for the library and demo app.
- Demo run, shadow jar, and jlink runtime-image tasks.
- Demo shadow jar excludes JavaFX artifacts.
- Demo shadow jar verification checks for an executable manifest and rejects bundled JavaFX entries.
- Local `releaseCheck` aggregates `check`, `shadowDemoJar`, and the default `jlinkDemoRuntime` so host-platform release validation has a single entry point without forcing all-platform JDK downloads.
- Publication metadata verification generates the Maven POM, rejects stale copied metadata, enforces the M3FX display name and optional static annotation metadata, and fails if JavaFX appears in published dependency metadata.
- Publication artifact verification checks the main jar, sources jar, and javadoc jar for required module and API entries, every bundled M3FX stylesheet resource, every main Java source entry, exported package documentation pages, and bundled JavaFX implementation classes.
- Publication layout verification cleans and publishes the Maven publication to a build-local verification repository, then checks the real Maven repository layout, including timestamped SNAPSHOT artifact names, for exactly one main jar, sources jar, javadoc jar, and POM, disabled Gradle module metadata, and accidental JavaFX artifacts.
- Publication consumption verification resolves the build-local Maven publication through a Gradle consumer runtime configuration, requires M3FX and MonetFX in runtime resolution, and rejects transitive OpenJFX artifacts.
- Publication verification avoids execution-time Gradle `Project` access and root-project self-coordinate resolution, so `check --warning-mode all` stays free of Gradle 10 deprecation warnings.
- jlink support uses BellSoft LibericaJDK Full target jmods, platform-specific archive formats, and an automatically downloaded host `jlink` when cross-platform or cross-architecture builds need a matching Java feature executable.
- Platform and architecture-specific jlink tasks cover Windows, Linux, and macOS on x64 and AArch64, including the aggregate all-platform/all-architecture release validation task.
- jlink runtime tasks verify generated image structure before succeeding, including release metadata, linked modules, JavaFX runtime metadata, JavaFX legal metadata, M3FX demo module presence, and platform launcher files.
- Runtime packaging choices for library usage, demo shadow jars, and jlink images are documented in `docs/PACKAGING.md`.
- GitHub Actions runs `releaseCheck` under Xvfb, then uploads the verified demo shadow jar as an unarchived artifact, plus visual snapshot PNGs and Gradle test reports for failed-run or manual visual review.

### Theme, Tokens, And Motion

- MonetFX-backed Material color mapping.
- Root CSS token generation for color, typography, shape, elevation, motion, state layers, density, and component defaults.
- Generated token stylesheets use stable content hashing.
- Component token factory tests generate unique token values and verify that every root component token flows into generated control rules, and that generated custom properties are consumed by production controls or bundled stylesheets.
- Standalone controls install a low-priority fallback token stylesheet on their scene and keep scene roots synchronized across runtime root replacement; dialog, menu, submenu, picker, and tooltip popup roots also match fallback token declarations, so per-control user-agent styles resolve default Material colors before an application theme is installed.
- Baseline and expressive profile hooks exist for shape and component token evolution.
- Reusable Material motion constants, easing curves, semantic specs, standard/expressive motion schemes, and motion behavior timings exist for JavaFX animations and motion-adjacent interaction delays.
- Runtime motion settings expose a settings revision and explicit change listeners so controls with long-running animation loops can refresh immediately when global or node-local animation switches, motion schemes, or behavior timings change.
- Scene-aware motion settings observers centralize listener registration for controls, skins, state layers, effect transitions, scroll smoothing, and popup/overlay animation owners, with explicit disposal for skin and helper lifecycles.
- State layers, ripples, and CSS-resolved elevation transitions resolve the installed theme motion scheme when available. Running state-layer opacity, ripple, and CSS-resolved effect animations settle immediately when inherited animation settings are disabled at runtime.
- State layer opacity is resolved from installed state-layer tokens plus owner interaction properties and pseudo-classes so hover, focus-visible, pressed, and armed feedback remain consistent across custom controls, themed overrides, and test-driven pseudo-class states.
- Skin-owned finite state transitions for selection controls, button pressed scale, sliders, tabs, navigation items, list items, disclosure icons, badges, and navigation drawer groups settle active animation frames immediately when inherited animations are disabled at runtime.
- Carousel selected-item scroll transitions observe runtime motion settings while attached to a scene, finish active scroll animations when inherited animations are disabled, and are verified through public viewport scroll positions rather than private animation fields.
- Control-owned finite overlay and popup transitions for scrims, sheets, snackbar hosts, search results, FAB menus, menu buttons, submenus, and picker fields settle active animation frames immediately when inherited animations are disabled at runtime. FAB menu expand and collapse transitions are verified through visible action opacity, scale, translation, reachability, and final expanded or collapsed public state instead of private animation references.
- Selection controls, indicators, text input details, popup surfaces, sheets, snackbars, search results, FAB menus, carousel scrolling, and determinate progress transitions now resolve semantic motion specs from the active motion scheme.
- Text input layout label, clear-button, and supporting-row transitions observe runtime animation settings while attached to a scene and settle active finite animations immediately when inherited animations are disabled.
- Popup-hosted menus, pickers, and tooltips propagate scene-level or local parent theme context, profile and brightness style classes, node orientation, animation settings, the active motion scheme, and motion behavior timings into their popup content roots, including runtime updates, scene-root replacement, and owner-subtree reparenting while the popup remains open.
- Dialogs inherit scene or owner-node local theme context while shown, including runtime owner scene changes, owner-scene root replacement, owner-window scene replacement, local theme changes, and owner-node reparenting, so picker dialogs and other dialog flows launched from locally themed subtrees keep the expected color and profile context.
- Tooltip timing, tooltip popup content, snackbar display duration, submenu hover delays, type-ahead search reset delays, indeterminate progress cycle durations, and loading indicator morph/rotation loops resolve profile-specific motion behavior timings where they are controlled by a scene owner.
- Type-ahead reset timers for static lists, virtualized lists, menus, and navigation drawers, submenu hover open and close timers, installed tooltip show, hide, and visible-duration timers, and snackbar automatic-dismiss timers refresh when inherited motion behavior changes while the owner remains attached to a scene. Tooltip, type-ahead, submenu, and snackbar timing-refresh coverage verifies real pointer or keyboard behavior plus public visibility, selection, queue, and stable current-state outcomes instead of reflecting private timers.
- Indeterminate linear progress, circular progress, and loading indicator loops refresh when inherited runtime animation settings and motion behavior timings change. Loading indicator morph segments settle before each interval ends and keep rotating during the hold phase, while disabling full motion settles determinate transitions and morphing effects but keeps reduced linear activity loops so indeterminate loading and progress states remain visibly alive.
- JavaFX scroll panes styled through `M3ScrollPanes` and virtualized list view flows support smooth wheel, focus, and programmatic scrolling that resolve the active motion scheme and animation settings, including horizontal-only content such as carousel viewports, explicit horizontal wheel deltas, horizontal character scroll units, Shift+wheel horizontal routing when the horizontal axis can scroll, vertical fallback when Shift is held on vertical-only content, platform text-line or page scroll units, and accumulated target values while a smooth-scroll animation is still running. Virtualized list smooth wheel scrolling is verified for accumulated wheel input plus platform text-line and page units in real windows. Library-owned carousel viewports use the same Material scroll styling and keep wheel ownership when nested inside outer page scroll panes. Outer smooth scroll panes now leave nested scroll owners, including `M3ListView`, text areas, nested `ScrollPane` content subtrees, JavaFX virtualized list/tree/table controls, and `VirtualFlow` targets, to handle their own wheel input. Running smooth-scroll animations settle immediately when inherited animation settings are disabled at runtime, restart with updated motion specs when motion settings change while animations remain enabled, and clear completed animation and callback references.
- Progress component tokens include wavy linear and circular amplitude, wavelength, track gap, and linear stop indicator metrics; loading indicator tokens include container size and active indicator size. Applications can override them per control through CSS.
- Button, FAB, icon, connected button group, segmented button group, icon toggle group, tab, chip, and chip group component tokens include Expressive-specific glyph sizing, horizontal padding, spacing, and tab active indicator metrics.
- Connected button group and split button component tokens include Expressive-specific grouped action padding and split menu width.
- Text input, text area, selection, and slider component tokens include Expressive-specific padding and interaction geometry.
- Navigation bar, navigation rail, navigation drawer, and list item component tokens include Expressive-specific content spacing, item spacing, and content padding.
- Menu, search, picker field, date picker, time picker, and sheet component tokens include Expressive-specific container padding, item spacing, result padding, day/time cell sizing, popup sizing, sheet padding, and drag-handle metrics.
- Card, dialog, snackbar, banner, tooltip, top app bar, bottom app bar, and toolbar component tokens include Expressive-specific shape, padding, height, content/action slot spacing, popup sizing, and top app bar variant metrics.
- Form and validation summary component tokens include Expressive-specific spacing, row sizing, label width, container shape, and invalid-item padding.
- Surface and carousel component tokens include Expressive-specific surface geometry and carousel track/item emphasis metrics.

### Component Coverage

- Foundation and utility visuals: text, icon, disclosure icon, avatar, badge, badged box, divider, surface, card, scrim, and sheets.
- Buttons and actions: button variants, icon button, icon toggle button, button groups, split buttons, FABs, extended FABs, and FAB menus.
- Inputs and forms: text fields, password fields, text areas, text input layouts, validation helpers, validation summaries, form containers, supporting text, errors, counters, leading and trailing adornments, and clear buttons.
- Selection: checkbox, radio button, switch, slider, chips, chip groups, segmented buttons, and segmented button groups.
- Pickers: date picker, date range picker, time picker, picker fields, preset actions, dialogs, range constraints, keyboard adjustment, and accessibility actions.
- Navigation: tabs, tab bar, navigation item, navigation bar, navigation rail, navigation drawer, collapsible navigation drawer groups, and type-ahead drawer navigation.
- App bars: top app bar variants, top app bar scrolled-under state, and bottom app bar with configurable FAB alignment.
- Toolbars: standard, floating, docked, horizontal, and vertical action toolbar variants with fixed Material action slots and a non-null public item-list contract.
- Menus and search: menu, menu item, submenu item, menu sections, menu button, type-ahead menu keyboard navigation, search bar, and search view with editor, result, and action focus routing.
- Feedback and progress: banner, snackbar, snackbar host, plain tooltip, rich tooltip, loading indicator default and contained variants, linear progress, and circular progress.
- Rich tooltips support keyboard transfer from the owner node into the action row, action-row traversal that skips disabled actions, boundary return to the owner, Escape dismissal, focus restoration, propagation of the active popup action focus through parent menu, banner, card, list item, dialog, surface, app bar, toolbar, and slot accessibility owners, and explicit reveal of reachable action targets from those composite owners.
- Expressive progress indicators render wavy active paths, separated tracks, and linear stop indicators while baseline progress indicators retain flat line and arc geometry.
- Lists: static list/list item support and `VirtualFlow`-backed list views with row reuse, selection, focus navigation, type-ahead navigation, visible-row accessibility focus, visible and distant virtual-row popup target reveal, default focus and reveal preservation for active visible-row popup focus, runtime theme-context refresh for materialized rows, accessibility routing, and a large two-line demo list that exercises real virtualized rendering.
- Slot, container, picker-grid, split-button, search, text-input-layout, navigation-drawer, validation-summary, banner, card, list-item slot, FAB-menu, snackbar-host, sheet, and popup-backed controls route accessibility focus to the currently focused child when focus is inside their indexed, adorned, grid, overlay, or popup-hosted content, route active external popup focus exposed by descendant controls through shared accessibility helpers, actively notify `FOCUS_NODE` when child or popup focus changes, preserve that current child for default `REQUEST_FOCUS` and parameterless `SHOW_ITEM` actions, and fall back to the selected or first reachable focus target when focus is outside the container. Actionable cards and ordinary list items preserve their own surface as the default accessibility focus target while still exposing current or explicitly requested slot/content focus. Shared reachability checks exclude hidden or disabled ancestor chains from focus and reveal candidates. Collection-owned selection and keyboard navigation also check owner reachability, so items belonging to hidden chip groups, toggle groups, segmented groups, tabs, navigation containers, lists, menus, carousels, and FAB menus do not remain selectable through public relative-selection APIs. Form validation focus, validation-summary accessibility items, search-result keyboard or reveal routing, search-result nested menu and tooltip reveal, search bar active focus and trailing popup reveal, text input adornment focus, list-item leading/trailing slot reveal, picker-field popup reveal, popup menu reveal, submenu reveal, split-button menu reveal, navigation-drawer collapsed-group menu and tooltip reveal, banner action-owned tooltip reveal, card content tooltip reveal, list-item slot tooltip reveal, sheet content menu and tooltip reveal, carousel item tooltip, nested-menu, and picker value reveal, dialog action-owned tooltip reveal, FAB menu nested action reveal, snackbar action-owned tooltip reveal, and FAB menu accessibility expansion use the same ancestor-chain reachability rules for hidden owners and hidden targets. Shared reveal helpers delegate explicit `SHOW_ITEM` requests into descendant controls that expose nested popup targets through their accessibility item tree, including ordinary content wrappers inside surfaces, cards, list-item slots, sheet content, carousel items, and dialog panes. Date range picker fields also track which endpoint opened the popup so Escape and accessible collapse return focus to the matching editor.
- Overlay and transient surface accessibility now distinguishes collapsed self-visibility from hidden ancestors: sheets and scrims keep offline structural reveal actions working, and parent containers can delegate indexed reveal requests into attached self-hidden sheet owners, while hidden ancestors block sheet, scrim, snackbar-host, and tooltip owner reveal or focus routing. Sheet focus-notification caches now mirror public `FOCUS_NODE` behavior by clearing exposed focus while the sheet is hidden.
- Indexed, slot-based, and popup-backed controls support default accessibility reveal behavior: parameterless `SHOW_ITEM` focuses the selected, current, or first reachable indexed child as appropriate for each control, direct child and descendant `Node` parameters are accepted when contained by an indexed item, and combined index-plus-target parameters continue reveal into nested targets after the indexed item is selected, expanded, or materialized. Menus and submenus prefer selected or active menu items when focus is requested, carousels scroll the selected item while restoring control focus, select the item that exposes an explicitly requested popup target, and delegate reveal into ordinary item content wrappers, virtualized list views restore the active row focus target and delegate explicit reveal requests into attached rows or materialized distant rows with nested popup targets, FAB menus expand before revealing direct actions or reachable nested action-owned popup targets, and snackbar hosts refresh hosted snackbar skins before routing focus to current or queued snackbar actions and their reachable action-owned tooltip targets. Snackbar queue promotion preserves focused action ownership when the dismissed snackbar action owned focus, while preserving unrelated page focus when snackbar actions were not focused.
- Accessibility reveal paths distinguish structural reachability from live scene focus: detached but visible structural owners can still resolve default reveal targets, activate search bars, promote queued snackbars, and report validation targets, while hidden or disabled ancestor chains continue to block real focus and reveal routing.
- Shared directional focus traversal rechecks caller-provided target lists at dispatch time, drops hidden, disabled, detached, and duplicate candidates, preserves explicitly supplied programmatic focus targets, and leaves events unconsumed when no reachable target remains.
- Right-to-left horizontal keyboard traversal mirrors the rendered visual direction for connected button groups, icon toggle groups, chip groups, segmented button groups, tab bars, navigation bars, toolbars, top app bars, bottom app bars, banners, structural surfaces, cards, badged boxes, sheet action rows, dialog action rows, search bar slots, text input layout slots, split buttons, carousels, menus, nested submenus, date pickers, and time pickers.

## Demo And Verification

- The demo app uses a Material navigation drawer sidebar with collapsible component groups aligned with the Material component organization.
- Demo pages cover major control families, common variants, disabled states, selected states, error states, composite workflows, and animated progress examples.
- Top app bars and bottom app bars are exposed as separate demo pages, with bottom app bar previews covering start, center, and end floating-action alignment.
- Date and time picker demo pages have dedicated visual coverage for fields, dialog launchers, inline pickers, range pickers, bounded variants, and rendered picker-cell text centering.
- Every registered demo page exposes a rendered `Material docs` action in its page header. Pages that do not have a dedicated current M3 component page are mapped to the closest active Material guidance page, and demo tests assert that the URL registry, URL shape, page registry, and per-page header action stay aligned.
- Progress demo pages explicitly show standard geometry alongside expressive wavy geometry using per-control CSS token overrides; the loading indicator page shows the dedicated loading control in the official default and contained forms.
- Unit tests cover style classes, token CSS metadata, skin creation, interaction events, state-layer/ripple behavior, accessibility attributes/actions, selection behavior, and packaging assumptions.
- Source style tests enforce local `@NotNullByDefault` declarations on every production and demo source type, reject `Optional` usage in those source roots, scan public controls for forbidden concrete JavaFX superclass inheritance and public `with`, `of`, or `create` factory methods, and scan public skins for forbidden concrete JavaFX skin inheritance or direct `Skin` implementation.
- Source style tests also verify that public styleable controls expose class and instance CSS metadata entry points, custom CSS metadata properties stay in the `-m3-` namespace, `isSettable` delegates through `M3Css.isSettable`, every referenced control stylesheet is bundled, every bundled control stylesheet is reached from source or base imports, bundled CSS token lookups resolve to generated root tokens or same-file local declarations, and token lookups are not quoted as JavaFX CSS string values.
- Snapshot-based visual tests render representative control families into report images, maintain a generated HTML snapshot index for manual review, and include automated checks for contrast, geometry, clipping, borders, rendered ink centering, and animation-state frames.
- Foundation and utility demo visual coverage now opens real Badges, Avatars, Dividers, Surfaces, Scrims, Forms, Typography, Icons, and Tooltips pages, verifies public control state matrices and concrete rendered geometry, writes dedicated snapshots, and catches compact badge, attached badge, avatar, divider, surface, scrim, form-row, text-role, icon, and tooltip-owner regressions.
- Recent visual regressions covered by tests include icon fallback glyph clipping, fixed-target rendered content centering, date and time picker cell ink centering, navigation icon-slot centering, fixed-height tab and segmented-button vertical centering, outlined text input notch geometry, focused outlined text input ink alignment with adornments and clear-button transitions, text input validation supporting-row opacity, bounds, and rendered-pixel stability during edits, text input runtime no-motion settling, skin-owned and control-owned state-transition settling, segmented button selected borders, progress geometry, selection states, snackbar sizing, dialog pane shape and popup surface continuity, trailing icon ripple behavior, and picker cell state-layer feedback for date, date range, and time pickers.
- Expressive profile visual coverage checks real rendered component sizes, action-control padding, input padding, selection/slider geometry, tab indicators, profile root classes, fixed-target centering, date-cell alignment, menu/search/sheet metrics, card/snackbar metrics, and search bar height constraints in mixed-height layouts.
- Dark expressive token-driven visual coverage renders every registered demo page without overriding generated dark theme colors, and asserts that the page list stays identical to the demo page registry.
- Dark expressive popup visual coverage opens real menu and tooltip popups, waits for the menu popup surface to settle through public opacity, scale, and translation state, checks inherited profile and brightness mode classes, and writes rendered snapshots for overlay review.
- Tooltip popup visual coverage opens plain, long, and rich action tooltips from real demo owners, verifies inherited popup theme style classes, owner-relative placement, compact popup geometry, wrapped long-text content, rich title/supporting/actions row geometry, transferred-pointer action clickability, and writes dedicated popup snapshots for review.
- Core tooltip visual coverage verifies popup root theme style classes, wrapped long-tooltip geometry, rich action containment, rendered text bounds, and reviewable popup snapshots in real JavaFX windows.
- Expressive progress visual coverage writes dedicated snapshots for determinate and indeterminate wavy linear and circular progress indicators, plus default and contained loading indicator presentation. Progress demo coverage now verifies every linear and circular sample's track, active segment, expressive wave, stop indicator, configured track-height envelope, and circular active/track rendered-pixel bounds across page-level standard, dark expressive, and right-to-left sweeps. Loading indicator demo coverage now verifies active-shape pixel centering, configured active-size envelopes, and contained background coverage in the same sweeps. Loading indicator core tests now anchor morph geometry to the polygon model center, generate exact morph frames through the shared polygon model, and verify sampled rendered centroids stay stable across morph segments without reflecting skin animation state.
- Demo interaction visual coverage checks hover and pressed-state snapshots for buttons, floating action buttons, extended FABs, split-button action and menu sides, menu buttons, top app bar actions, toolbar actions, banner actions, dialog actions, text-input clear buttons, text-input trailing action buttons, picker-field open buttons, chips, segmented buttons, tabs, navigation items, list items, checkbox controls, radio buttons, switches, sliders, sidebar destinations, and toggle icon buttons, verifies button, floating action button, extended FAB, split-button action and menu side, menu button, top app bar action, toolbar action, banner action, dialog action, text-input trailing action, picker-field open button, chip, segmented button, tab, navigation item, list item, checkbox, radio button, switch, toggle icon button, and sidebar ripple-release intermediate fade frames, and verifies clear-button removal after click, chip, segmented-button, tab, navigation-item, and list-item selected states, captures real slider press and drag frames, captures switch thumb-selection plus navigation bar, navigation rail, sidebar drawer-group, bottom sheet, and side sheet intermediate frames with local visual-test motion schemes, waits for navigation selected indicators to expose real partial opacity/scale animation states before accepting intermediate frames, opens split button, picker field, and nested submenu popups for enter, settled, and exit snapshots, and covers focus feedback for text fields plus no-motion state changes.
- Overlay visual coverage now exercises snackbar host enter/settled/exit frames, snackbar action hover/pressed/ripple-release frames, bottom-sheet and side-sheet action hover/pressed/ripple-release frames, FAB menu expand/collapse and action hover/pressed frames, rich tooltip interactive pointer transfer with action-button containment plus action hover/pressed frames, real dialog popup surface snapshots, and pixel checks that reject JavaFX default dialog header/content strips.
- Demo page-level visual coverage now shares geometry assertions across full-page, dark expressive, and right-to-left passes, waits for the demo sidebar selected indicator to settle before full-page snapshots, verifies visible text, Material control bounds, scene-snapshot rendered text ink centering in fixed targets including tabs, single-line text input ink measured from the real scene snapshot, one-line list/menu/sidebar headline ink centering when extracted pixels are representative, list/menu/sidebar overline, headline, supporting, and trailing-supporting text containment and vertical ordering, list/menu/sidebar leading and trailing slot containment, vertical centering, fixed token sizes, nested SVG icon centering, SVG icon viewport centering against layout bounds, date and time picker cell centering, tab active-indicator bottom anchoring and selected-pixel coverage, tab LTR/RTL physical ordering, navigation bar and rail item token geometry, navigation selected-indicator local token sizing and selected-pixel coverage, navigation item label containment, navigation item indicator and graphic-slot centering, compact navigation badge placement, navigation drawer selected-row container pixels and resolved row-height tokens, demo sidebar top-level and grouped child selected-pill indentation, and scroll viewport clipping while allowing expected partial scroll-edge content and carousel viewport clipping covered by dedicated carousel tests. The right-to-left pass now covers every registered demo page and asserts that the RTL page list stays identical to the demo page registry.
- Registered demo page smoke coverage opens every page in standard, dark expressive, and right-to-left modes through the sidebar-settled page switch helper while CSS warnings are captured, then verifies the current page title, selected sidebar title, mode-specific root state, Material documentation URL contract, and rendered docs-link geometry. This keeps ordinary page-open regressions in token lookups, Paint conversion, sidebar selection styling, theme mode propagation, and node-orientation propagation visible before broader visual sweeps run.
- App bar visual coverage renders all top app bar variants and scrolled-under states with real SVG action icons, writes dedicated top-variant and scrolled-under snapshots, uses the current Material app bars documentation link, verifies logical icon identities, rejects rendered text placeholders inside icon graphics, verifies leading and trailing icon color roles, 48 dp top app bar action slots, icon button centering, action slot spacing, title and icon-row geometry, RTL mirroring, contextual top app bar preview content, default top app bars sharing the content surface until scrolled-under elevation applies, and preview surfaces that avoid the generic rounded showcase container.
- Core action-container coverage verifies runtime LTR/RTL/LTR slot reflow for top app bar navigation and trailing actions, bottom app bar regular and floating actions, banner icon and action items, sheet and dialog action rows that do not steal content editing keys, text input layout adornment slots that do not steal editor navigation keys, toolbar horizontal and vertical item traversal, FAB menu toggle-anchored vertical traversal, Material action-slot geometry, and current focused action preservation in real JavaFX windows.
- Toolbar visual coverage renders standard, floating, docked, and vertical `M3Toolbar` examples with real SVG icon buttons, verifies toolbar variants, orientation, container and item-slot geometry, centered icon viewports, Material documentation linking, and confirms the Toolbars demo page no longer reuses bottom app bar examples. The Toolbars snapshot is captured after a stable pulse boundary so the visual artifact reflects the newly displayed page instead of the previous rendered frame. Core toolbar layout tests also verify real-window right-to-left visual mirroring and snap-aligned action slot geometry.
- Cards demo visual coverage opens the real Cards page, verifies filled, outlined, elevated, media, action, and disabled states, checks card surface geometry and variant treatment, and writes dedicated top and scrolled media/action snapshots for review.
- Carousel demo visual coverage opens the real Carousel page, verifies multi-browse and compact carousel viewports, selected item state, selected item layout visibility inside the viewport, action-driven selection changes, intentional viewport clipping, and writes dedicated initial and after-next-action snapshots for review.
- Dialogs demo visual coverage opens the real Dialogs page, verifies launcher buttons, inline basic/form/scrollable dialog panes, Material action buttons, custom content bounds, and writes dedicated top and scrolled dialog snapshots for review. Dialog pane actions use a fixed Material order instead of platform-specific JavaFX button ordering, and dialog pane component-token tests assert that CSS and runtime container-shape changes update the actual rendered background radii.
- Search demo visual coverage opens the real Search page, verifies standalone and embedded search bar slot order and centering, active search view surface, embedded bar, result container, contiguous two-line result-row geometry, inactive search view collapsed layout, active and inactive result visibility, keyboard reachability from the editor into the first result row, and writes a dedicated active-search snapshot for review. Core search keyboard coverage verifies slot traversal for leading, editor, and trailing action targets without stealing editor navigation keys, including embedded search bars inside search views and runtime right-to-left mirroring.
- Text Fields demo visual coverage opens the real Text Fields page, which now includes a dedicated right-to-left filled, outlined, and password section, verifies filled, outlined, validation, error, password, counter, clear-button, adornment, text-area, and RTL layout geometry, checks single-line input ink with real rendered pixels, checks outlined floating labels use open notch geometry and screenshot-sampled transparent label backgrounds instead of background masks, verifies active validation feedback stays settled through invalid/valid/invalid edits without replaying supporting-row entry motion, and writes top layout, validation-stability, and scrolled RTL geometry snapshots for review.
- Selection demo visual coverage opens the real Checkbox, Radio Button, Switch, and Slider pages, verifies enabled, disabled, selected, unchecked, indeterminate, continuous, discrete, vertical, and mirrored right-to-left states, checks checkbox mark centering and rendered check-or-dash pixels, radio dot centering, switch thumb placement, slider track/thumb value geometry, and slider preferred-size preservation under token/theme updates, and writes dedicated state snapshots for review.
- Chips demo visual coverage opens the real Chips page, verifies assist, suggestion, input, filter, selected, disabled, multi-select, and single-select states, checks each chip's resolved container height, rendered outline or selected-fill footprint, selected versus transparent interior pixels, rendered label ink centering, leading SVG icon containment, and chip-group child containment across standard, dark expressive, and right-to-left sweeps.
- Menus demo visual coverage opens the real Menus page after the sidebar selection indicator settles, verifies inline menu selected and multi-selected states, opens the menu-button popup and nested submenu with inherited theme context, waits for owner and nested popup roots to reach stable rendered state, checks compact side-by-side popup geometry, verifies section headers, dividers, menu row heights, leading and trailing slots, selected-container layout and pixels, submenu indicators, SVG icon slots, and writes dedicated inline, owner-popup, and submenu snapshots for review.
- List visual coverage opens the real Lists demo page after the sidebar selection indicator settles, verifies that the data-driven list is backed by `VirtualFlow`, asserts that only a bounded number of reusable cells are attached for 240 rows, scrolls to a far row synchronously, and writes a dedicated virtualized-list snapshot for visual review.
- State-layer and ripple motion tests inspect expansion, disabled-motion settling, runtime animation-setting changes, and reduced indeterminate activity loops through public rendered state, real JavaFX pulses, or shared model geometry rather than private animation fields. Core state-layer ripple tests now verify held ripples, early release expansion, fade-out frames, and final clearing through real JavaFX pulse waits against visible opacity and scale changes instead of reflecting ripple timelines. Core button ripple release tests verify long-press and quick-click fade-out through real JavaFX pulse waits against visible opacity changes. Button pressed-scale coverage verifies non-elevated buttons, elevated buttons, flat labeled controls, icon toggle buttons, and floating action buttons through real JavaFX windows and stable pulse-driven `scaleX`/`scaleY` states instead of jumping private skin timelines to fixed frames.
- Core indeterminate progress and loading indicator runtime-motion tests now verify enabled, reduced, and restored activity through real JavaFX pulse movement of rendered bar positions, expressive linear wave/track separation, circular start angles and cycle-seam continuity, loading indicator path points, distinct loading frames, path element reuse, and reduced-motion rendered ink stability rather than private timeline status checks.
- Core selection indicator animation coverage now uses observable local motion schemes and state-driven JavaFX pulse waits for checkbox marks, radio dots, switch thumbs, and checkbox indeterminate-to-selected mark replay instead of reflecting those private selection timelines.
- Core segmented button, list item, navigation drawer, tab, navigation bar, and navigation rail selection animation coverage now opens real JavaFX windows with observable local motion schemes and waits for rendered opacity/scale state changes instead of jumping private selection or indicator timelines to fixed frames.
- Navigation drawer group child selection geometry is verified in left-to-right, right-to-left, and runtime LTR/RTL/LTR orientation changes, including selected pill indentation, edge alignment, width, row height, and shape radii.
- Core skin-owned disabled-motion settling coverage now waits for real intermediate rendered frames across selection controls, segmented buttons, sliders, tabs, navigation items, list items, disclosure icons, and badges before disabling motion and asserting that every visible state settles immediately.
- Core overlay-owned disabled-motion settling coverage now waits for real intermediate rendered frames across scrim opacity, sheet and search-result exit transforms, snackbar entrance transforms, and FAB-menu action entrance transforms before disabling motion and asserting that every overlay-owned state settles immediately without reapplying CSS over active animation properties.
- Card behavior tests cover passive versus actionable accessible roles, automatic focus traversal for actionable card surfaces, handler replacement, and restoration of the previous focus-traversal state when a card becomes passive again.
- Core popup, overlay, snackbar, tooltip, sheet, scrim, search, progress, submenu, drawer-group, standalone fallback stylesheet, and detached virtualized-list completion tests prefer state-driven JavaFX condition waits over hard-coded elapsed-time waits where the controls expose observable state. Tooltip show, tooltip visible-duration, tooltip hide, type-ahead reset, and submenu hover timing refresh tests now prove inherited motion behavior changes through real JavaFX pointer or keyboard interaction results rather than private `PauseTransition` fields, and tooltip popup tests verify owner node-orientation propagation plus scene and local parent runtime theme reinheritance while the popup remains visible. Menu button, submenu, picker field, and date range picker field popup tests verify runtime scene or parent-popup theme reinheritance while those popup roots remain visible. Shared popup context tests verify scene-root replacement, owner subtree reparenting, runtime local-theme installation and removal after synchronization starts, and stopped synchronizers no longer observing later scene or local theme changes. Virtualized list tests verify visible materialized rows reapply scene or local parent theme context at runtime and clear copied context when a local theme is removed. Snackbar display-duration refresh tests prove explicit and inherited behavior-duration changes through real automatic dismissal and queue promotion, while indefinite display durations are checked across stable JavaFX pulses. Carousel selected-item scroll settling now waits for a real intermediate viewport `hvalue` before disabling animations and verifying the final viewport position. FAB menu expand/collapse lifecycle coverage now waits for real action intermediate frames and settled public states. Menu button, submenu, date picker, and date range picker popup-surface disabled-motion tests now wait for a real enter-transition frame before asserting immediate identity-transform settling when inherited animations are disabled. Virtualized list view and styled JavaFX scroll pane smooth-scroll completion tests now wait for the rendered target scroll position to remain stable across consecutive pulses instead of inspecting private animation fields. Demo visual tests also support stable-condition waits that require final popup, sheet, snackbar, and FAB menu states to remain true for consecutive JavaFX pulses before settled snapshots are captured.
- Demo animation and visual tests capture progress, loading indicator, split-button popup, picker-field popup, nested submenu, hover/pressed interaction feedback, switch selection, navigation selection, drawer group disclosure, sheet visibility, snackbar, FAB-menu, and carousel action frames after real rendered pixel or semantic state changes are detected, so those snapshots no longer rely on fixed early popup, generic interaction, ordinary page-layout, or broad midpoint transition delays. Progress and loading indicator coverage now requires two consecutive batches of four unique advancing rendered frames, so tests fail if a long-running animation only moves briefly and then stops. Loading indicator demo coverage also checks rendered pixel centroid stability across captured frames, with animation-frame geometry reads performed on the JavaFX application thread. Popup settled snapshots for standalone popup scenes require stable node snapshots, not just opacity and transform state, before writing menu, submenu, split-button, picker popup, rich tooltip, and dialog popup artifacts. Switch, navigation, drawer group, sheet, snackbar, and FAB-menu final-frame captures require the relevant semantic state plus stable rendered node-area pixels, so settled snapshots are not accepted just because a second intermediate frame appeared. Ripple release coverage combines rendered release-frame pixel changes with semantic opacity fade checks, rich tooltip lifetime coverage relies on stable popup ownership and hidden-state conditions after pointer transfer, and disabled-motion interaction coverage waits for the demo page selection state to settle before sampling immediate hover feedback. Page-wide demo visual guards now verify centered rendered text ink for button-like `Labeled` controls and include more Material control families in scene and viewport bounds checks across standard, dark expressive, and right-to-left captures. Core and demo visual test utilities no longer expose public fixed-delay FX sampling helpers; remaining time values are component motion configuration, CSS warning drains, or state-driven pulse waits.
- Every registered demo page now has a required page-specific visual-state assertion. The standard, dark expressive, and right-to-left all-page demo sweeps run those assertions in addition to shared geometry checks, so new demo pages cannot silently opt out of component-specific visual validation.
- Button-family demo visual coverage now verifies concrete state matrices for buttons, connected button groups, segmented buttons, icon buttons, toggle icon buttons, FABs, extended FABs, FAB menus, and split buttons, including variant counts, disabled states, group item contracts, SVG icon usage, split-button part bounds, segmented-button rendered outline and selected-fill footprints, selected-container insets, selected versus transparent interior pixels, physical edge style classes, adjacent segment border sharing, and reviewable page snapshots.
- Sheet and snackbar demo visual coverage now verifies bottom-sheet, side-sheet, snackbar message, snackbar action, and queued-snackbar state matrices with reviewable PNG snapshots, compact snackbar surface geometry, sheet list content containment, sheet action containment, variant contracts, and queue state assertions.
- Core and demo text input visual checks now measure real rendered text ink inside single-line filled, outlined, password, and RTL input layouts, distinguish filled floating-label content slots from outlined centered slots, verify outlined floating labels use opened outline notches instead of rectangular background masks in page-level standard, dark expressive, and right-to-left sweeps, and write reviewable PNG snapshots for focused outlined, supporting/error/counter, and right-to-left text-field states.
- Core text input animation coverage now uses observable local motion schemes and state-driven JavaFX pulse waits for floating labels, clear buttons, supporting rows, disabled-motion settling, and trailing icon ripples instead of reflecting those private timelines.
- Text input layout RTL coverage verifies static and runtime LTR/RTL/LTR reflow for leading and trailing adornment slots, optional trailing-slot geometry, floating-label notch geometry, supporting text, counters, wrapped input node orientation, rendered text ink containment, outlined text centering, SVG adornment slots, and fixed-target icon-button centering.
- Search bar and search view RTL coverage verifies runtime LTR/RTL/LTR reflow for leading and trailing action slots, embedded active-search result orientation, result-row trailing content, keyboard traversal between the editor and results after orientation changes, and current child focus preservation in real JavaFX windows.
- Core and demo visual wait helpers report pixel-change counts, thresholds, node bounds, popup attachment state, baseline availability, condition-evaluation counts, true and false condition totals, last condition result, pulse counts, last pulse timestamp, and stable-pulse progress when animation or rendered-state waits time out. They preserve condition-specific diagnostics even if pulse delivery stops, restore global motion settings after pulse-driven setup, verification, and failure paths, and use test-only short-timeout paths to verify one-shot and stable timeout diagnostics without adding slow failure tests.
- Core FX wait helpers accept condition-specific timeout diagnostics and preserve those diagnostics even if pulse delivery stops before an `AnimationTimer` reports its own timeout. High-risk FAB menu, carousel scroll, snackbar, and sheet animation waits now report current visibility, managed state, opacity, transform, scroll, host, queue, and attachment details on timeout.
- Source-level visual wait discipline tests dynamically discover rendered-image, pixel, and snapshot regression test sources across core and demo test roots, require the known core date-picker, date-range-picker, time-picker, and demo visual suites to remain covered, require those suites to keep producing reviewable PNG report snapshots, verify direct snapshot writes target `build/reports`, require standard, dark expressive, and right-to-left full-page demo sweeps to keep stable page switching, reviewable snapshots, shared geometry assertions, and page-specific visual-state assertions, require core and demo FX wait helper timeout diagnostics to stay covered, and reject fixed-time sleeps, ad-hoc pause transitions, parking primitives, direct visual-test `Timeline`, `KeyFrame`, or `AnimationTimer` waits, and direct visual-test `Platform.runLater` or `CountDownLatch.await` scheduling so visual and animation coverage stays tied to semantic state, stable pulses, or rendered pixel changes.
- Source-level API contract tests require public concrete controls that explicitly assign accessible roles, plus concrete `M3PickerField` subclasses that inherit their role from the picker-field base, to be covered by the core `controlsExposeAccessibilityRoles` matrix. The matrix now includes date pickers, time pickers, picker fields, navigation drawer groups, and validation summaries so newly added accessible controls cannot silently skip the baseline role contract.
- Source-level style contract tests require public concrete controls that declare `STYLE_CLASS` or own or inherit a user-agent stylesheet to be covered by dedicated core style-class and stylesheet matrices. The checks only accept real `getStyleClass().contains(...)` and `assertUserAgentStylesheet(...)` assertions inside the matrix methods, which now include action controls, picker fields, progress and loading indicators, form containers, toolbars, dialog panes, password fields, icon buttons, and inherited button stylesheet coverage for menu buttons so new public controls cannot silently skip baseline Material styling entry points.
- Source-level hit-target contract tests derive public concrete `ButtonBase` descendants and `M3ListItem` action-row descendants through the controls inheritance graph, require `ButtonBase` controls to appear in the core full-bounds hit-target behavior matrix, and require button-like/action-row controls to enable or inherit `setPickOnBounds(true)`, so new interactive controls cannot silently skip pointer activation coverage.
- Core action hit-target tests verify full layout-bounds pointer activation for standalone buttons, icon buttons, FABs, icon toggles, chips, segmented buttons, tabs, navigation items, selection controls, switches, list items, and connected action groups in both left-to-right and right-to-left orientation through node-aware scene-coordinate mouse events.
- Keyboard traversal tests cover right-to-left horizontal focus and selection behavior across action groups, selection groups, tabs, navigation bars, top app bars, bottom app bars, banners, structural surfaces, cards, badged boxes, sheet action rows, dialog action rows, search bar slots, text input layout slots, toolbars, split buttons, carousels, date pickers, and time pickers. Action-container tests cover Home/End, hidden and disabled action skipping, RTL horizontal mirroring, top app bar navigation slots, bottom app bar floating-action slots, banner icon/action traversal, sheet and dialog action rows that do not steal content editing keys, search bar slots that do not steal editor navigation keys, text input layout adornment slots that do not steal editor navigation keys, top app bars, bottom app bars, banners, and toolbars that do not steal embedded text input navigation keys, toolbar horizontal and vertical traversal through a shared directional helper, FAB menu toggle-anchored vertical traversal from outside focus, disabled FAB action skipping, and current focus-node preservation in real JavaFX windows. Structural container tests cover surface content traversal, badged-box content-to-badge traversal, actionable card surface-to-content traversal, RTL mirroring, hidden and disabled target skipping, and text input editing-key preservation in real JavaFX windows. Toolbar tests also cover vertical traversal, hidden and disabled item skipping, nested item focus targets, popup-backed nested menu reveal, popup focus routing, hidden-owner reveal rejection, right-to-left rendered action order, runtime item-slot rebuilding in real windows, and direct item-list null rejection before skin state can be rebuilt from invalid children. Search control tests cover editor, result, direct and descendant trailing-action focus routing, popup-backed leading/trailing action reveal, result-row menu and rich-tooltip reveal, current child preservation for default focus/reveal actions, complex result-row descendant focus routing, result-collapse focus restoration, unreachable result skipping, result-list Home/End/PageUp/PageDown movement, search bar leading/editor/trailing slot traversal, embedded search bar slot traversal inside search views, and hidden-ancestor result or owner filtering. Picker-grid tests cover current visible date, range-date, and time cell preservation for default focus/reveal actions in real window focus scenarios. Picker field and dialog preset tests cover vertical preset action traversal, PageUp/PageDown movement, hidden-owner focus and popup reveal rejection, left-to-right or right-to-left handoff from preset columns into adjacent pickers, and open preset popup reflow with focus preservation across inherited runtime LTR/RTL/LTR orientation changes for date, time, and date-range fields. Text input layout tests cover input, leading adornment, trailing adornment, clear-button accessibility focus routing, slot keyboard traversal, RTL slot mirroring, current adornment preservation for default focus/reveal actions, compound adornment descendant focus routing, popup-backed adornment reveal, hidden-owner focus rejection, and focus restoration when a focused adornment disappears. Form container tests cover row horizontal traversal, section and pane vertical traversal, hidden and disabled form child skipping, non-wrapping boundary handoff through nested form containers, and text input editing-key preservation; form validation tests cover first-invalid focus fallback from hidden invalid layouts to reachable invalid layouts. Slot/container tests cover current-child accessibility focus routing, active `FOCUS_NODE` notifications, hidden-ancestor reachability filtering, and default focus/reveal preservation for app bars, banners, surfaces, forms, sheets, split buttons, button groups, navigation drawers, navigation drawer groups, validation summaries, and badged boxes. Validation-summary tests cover rendered invalid-row Up/Down/Home/End traversal, Enter/Space activation into the referenced field, hidden invalid inputs, hidden summary owners, reachable accessibility item indexing, and explicit reveal rejection for hidden targets. Popup-backed menu, submenu, split-button menu, picker field, date range picker field, time picker field, dialog pane, FAB menu, and rich tooltip tests cover cross-scene focus routing, action-row traversal, dialog action-row rich-tooltip focus and reveal, Escape dismissal, hidden-owner reveal rejection, accessible collapse focus restoration, and focus restoration from real child focus targets. Modal sheet tests cover accessible collapse focus restoration and hidden-sheet focus target clearing. Picker field preset popups, date range picker endpoint restoration, dialog-hosted date range picker fields, and surface-hosted time picker fields now cover nested popup focus routing from real popup focus targets. Navigation drawer tests cover collapsible group disclosure keys, group-row descendant `FOCUS_NODE` routing, collapsed-group descendant reveal actions, collapsed-group nested menu and rich-tooltip reveal actions, current child default focus preservation, active expanded-group child popup focus preservation, and focus restoration in left-to-right and right-to-left layouts. Static lists, menus, navigation drawers, and virtualized lists cover disabled and hidden row skipping, PageUp/PageDown focus movement, rendered row descendant accessibility actions, visible row popup target reveal, and distant virtual row materialization before nested menu or picker reveal. Shared selection navigation and grouped selection tests cover candidates hidden through item ancestors and owner ancestors. Picker skin tests cover date, range-date, and time cells hidden through ancestor containers. Composite and structural container tests cover parameterless, indexed, direct-node, descendant-node, and picker value-target accessible reveal actions across button groups, list panes, app bars, banners, surfaces, form rows, badged boxes, FAB menus, carousels, navigation drawers, and snackbar hosts.
- Custom indexed-control accessibility tests cover parameterless reveal behavior for carousels and virtualized list views, carousel item-wrapper date and time picker value reveal, including real scene focus restoration.
- Shared `M3FocusTraversal` coverage verifies that embedded text inputs keep focus while parent directional traversal receives bubbling or direct child key events, compound target lists prefer exact focused descendants before ancestor containers, actionable cards can move focus between the card surface and nested content before JavaFX default traversal escapes the card, actionable card and list item activation handlers ignore Enter/Space while nested text inputs own focus, and carousel, static-list, and virtualized-list coverage verifies the same behavior for indexed navigation and type-ahead handlers.
- Selection-container accessibility tests cover current-child `FOCUS_NODE` routing and parameterless reveal behavior for chip groups, icon toggle groups, segmented button groups, list panes, tab bars, navigation bars, navigation rails, and navigation drawers, ensuring real focused children are reported first and selected items are preferred over the first child when focus is outside the container.
- Popup menu accessibility tests cover selected-item default focus for standalone menus, menu buttons, and submenus without changing explicit first/last keyboard-open behavior. Popup menu keyboard tests also cover two-level nested submenu traversal, `FOCUS_NODE` propagation through active popup branches, close-key focus restoration, and runtime right-to-left node-orientation changes while the popup stack remains open, including inherited owner-direction changes while every nested submenu popup is already showing. Mixed popup focus tests cover rich tooltip actions opened from menu items, nested submenu items, split-button menu items, invalid text-field adornments, form rows, banner actions, card content actions, list item slot actions, sheet content, search actions, search result rows, navigation drawer group rows, app bar actions, toolbar actions, FAB menu actions, and snackbar actions while the parent menu stack, split-button menu stack, validation summary, form pane/section/row chain, action banner, card content surface, list row, shown sheet, search owner, drawer owner, app bar, toolbar, action menu, or snackbar host remains active, propagate the focused tooltip action through menu, submenu, split button, validation summary, text input layout, form rows, form sections, form panes, banner, card, list item, sheet, search bar, search view, navigation drawer group, navigation drawer, top app bar, bottom app bar, toolbar, FAB-menu, snackbar-host, dialog, surface, and slot owners, reject unreachable rich-tooltip action reveal requests, route shown and hidden bottom-sheet and side-sheet content menu, rich-tooltip, date-picker, time-picker, and date-range picker popup focus through the sheet owner while restoring focus after Escape, route dialog-pane focus through nested menu-button, split-button, rich-tooltip, and picker popups, route shared external popup focus and explicit reveal requests through indexed and selection containers, form rows, form sections, form panes, validation summaries with date-picker, time-picker, and date-range picker targets, surface content subtrees, badged boxes, banners, cards, list items, sheets, carousel item content, search controls, navigation drawers, top app bars, bottom app bars, toolbars, and slot-based app bars, directly verify nested menu popup targets from icon toggle groups, segmented groups, chip groups, tab bars, navigation bars, navigation rails, list panes, and carousels, verify carousel item content rich-tooltip, active date-picker and time-picker popups, date-picker value, time-picker value, and date-range picker value routing, and explicitly reveal closed nested menu, rich-tooltip action, split-button menu tooltip action, split-button nested submenu picker and date-range-picker targets, FAB-menu time-picker and date-range picker targets, nested submenu picker and date-range-picker value targets, invalid input adornment menu, date-picker, time-picker, date-range picker, and tooltip targets, form-row menu, date-picker, time-picker, date-range picker, and tooltip targets, search trailing menu, tooltip, date-picker, and time-picker targets, search result menu, tooltip, date-picker, and time-picker targets, navigation drawer collapsed-group menu and tooltip targets, banner action tooltip, card content tooltip, list-item slot tooltip, sheet content menu, tooltip, time-picker, and date-range picker targets, app bar navigation tooltip, app bar action tooltip, bottom app bar floating-action menu and tooltip targets, toolbar action tooltip, FAB-menu nested tooltip action, snackbar action tooltip, queued snackbar host actions, and picker value targets from those composite owners. Dialog pane accessibility tests cover current content/action preservation for parameterless focus and reveal actions, including picker dialog preset and picker-content targets plus nested picker popups exposed through ordinary content containers. Picker field accessibility tests cover open-button focus exposure and preservation of focused preset actions for direct `REQUEST_FOCUS` and parameterless `SHOW_ITEM` actions.
- Mixed popup runtime orientation coverage verifies active nested submenu rich tooltip and picker popup focus chains through left-to-right, right-to-left, and restored left-to-right owner direction changes while the popup stack remains open.
- Picker field mixed-popup accessibility coverage now verifies top app bar action slots, bottom app bar action slots, toolbar item slots, search bar trailing action slots, search view embedded search bar slots, and search view result content can reveal date-picker, time-picker, and date-range picker value targets in both search-bar and result-content paths, route popup focus through the composite owner, and restore editor focus or the owning date-range endpoint after Escape. Search bar and search view accessibility reveal now enter active state without stealing focus from an explicitly revealed trailing-slot child, and accessibility target matching walks logical `ITEM_AT_INDEX` children as well as rendered parent children.
- FAB-menu mixed-popup coverage now verifies action-owned menu popup targets, action-owned date-picker, time-picker, and date-range picker value targets, and action-owned rich tooltip targets route focus through both the expanded FAB menu and an outer composite surface. The FAB menu itself also preserves active action-owned popup focus for default `REQUEST_FOCUS` and parameterless `SHOW_ITEM` accessibility actions.

## Next Goals

- Keep visual and animation tests tied to semantic states, stable pulses, or rendered-pixel changes as new interaction coverage is added.
- Continue filling component gaps and richer composite workflows where existing controls are still shallow.
- Audit less common mixed popup stacks for keyboard parity, especially nested overlay combinations that combine menus, pickers, tooltips, and dialogs.
- Improve M3 Expressive parity with additional profile-specific component tokens, expressive containment, and visual verification for remaining component families as target values become stable.

## Current Verified Gates

- Current working tree verification:
  - `./gradlew -g .gradle-user-home check` passes, including library tests, Javadoc generation, publication metadata checks, build-local Maven publication layout checks, and Gradle consumer resolution.
  - `./gradlew -g .gradle-user-home releaseCheck` passes, including `check`, executable demo shadow jar verification, and the default host-platform demo jlink runtime image with built-in runtime image structure verification.
  - `./gradlew -g .gradle-user-home :demo:compileTestJava` passes as a focused compile gate for the expanded visual smoke test source.
  - `./gradlew -g .gradle-user-home :test --tests org.glavo.m3fx.VisualWaitDisciplineTest` passes, including source-level full-page demo visual sweep coverage discipline.
  - `./gradlew -g .gradle-user-home compileTestJava` passes after the mixed-popup and carousel accessibility regression expansions.
  - `./gradlew -g .gradle-user-home :test --rerun-tasks --tests org.glavo.m3fx.controls.M3MixedPopupFocusTest` passes, covering bottom-sheet and side-sheet nested menu, rich-tooltip, date-picker, time-picker, and date-range picker popup focus and reveal parity, plus carousel item content tooltip, active date-picker and time-picker popups, date-picker value, time-picker value, and date-range picker value routing.
  - `./gradlew -g .gradle-user-home :demo:test --tests org.glavo.m3fx.demo.M3FXDemoVisualSmokeTest.overlayDemoSurfacesRenderAndAnimate --tests org.glavo.m3fx.demo.M3FXDemoVisualSmokeTest.interactiveDemoStatesProduceVisibleFeedback` passes, covering overlay enter/exit frames, action hover/pressed feedback, ripple-release fade frames, slider drag frames, selection-state frames, popup frames, focus feedback, and no-motion state changes.
  - `./gradlew -g .gradle-user-home :demo:test --rerun-tasks --tests org.glavo.m3fx.demo.M3FXDemoVisualSmokeTest` passes, covering the full standard, dark expressive, and right-to-left demo visual sweep.
  - `./gradlew -g .gradle-user-home jlinkDemoAllPlatformArchitectureRuntimes --rerun-tasks` passes, rebuilding Windows, Linux, and macOS demo runtime images for both x64 and AArch64 targets while executing the built-in release/module/JavaFX metadata and launcher structure verification for every generated image.
  - IntelliJ IDEA errors-only inspection passes for `build.gradle.kts`, `src/main/java/org/glavo/m3fx/controls/M3Accessible.java`, `src/test/java/org/glavo/m3fx/controls/M3ControlStyleTest.java`, `demo/src/test/java/org/glavo/m3fx/demo/M3FXDemoVisualSmokeTest.java`, and `src/test/java/org/glavo/m3fx/controls/M3MixedPopupFocusTest.java`.
  - Generated PNG snapshot pixel-diff checks confirm visible differences for bottom-sheet action, side-sheet action, snackbar action, FAB menu action, and rich tooltip action normal/hover/pressed or ripple-release frames.
- No additional release verification gate is currently pending after the latest full visual and all-platform runtime sweeps.

## Validation Entry Points

- `compileJava` validates main source compilation.
- `check` validates generated publication metadata, publication jar contents, build-local Maven publication layout, and Gradle consumer resolution, then runs the default verification lifecycle.
- `javadoc` generates the public API documentation artifact.
- `test` validates unit, behavior, snapshot, and packaging tests.
- `releaseCheck` validates the library publication, executable demo shadow jar, and default demo jlink runtime image structure together.
- `shadowDemoJar` validates executable demo jar packaging without bundled JavaFX.
- `jlinkDemoRuntime` validates the default demo runtime image.
- Platform jlink tasks validate runtime images for Windows, Linux, and macOS.
- Architecture jlink tasks validate fixed x64 and AArch64 runtime images for each supported platform.

## Out Of Scope For Now

- Full M3 Expressive visual parity for every component.
- Web deployment for the JavaFX demo.
- SASS or another CSS preprocessor layer.
- High-complexity data components such as data tables.
